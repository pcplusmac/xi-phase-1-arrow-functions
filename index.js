// define an arraow function
const add = (a, b) => a+b;
add(2,3);

// define function divide using function expression
const divide = function (a, b) { return a/b};
divide(2000,100);

// define arrow function square
const square = x => x*x;